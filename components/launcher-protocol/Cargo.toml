[package]
name = "habitat-launcher-protocol"
version = "0.1.0"
authors = ["Jamie Winsor <reset@habitat.sh>"]

[dependencies]
protobuf = "*"
serde = "*"
serde_derive = "*"

[build-dependencies]
pkg-config = "0.3"

[features]
protocols = []
